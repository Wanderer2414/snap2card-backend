# LLM config is read from the certs/ directory (secrets, gitignored):
#   certs/openai_api_key.txt  -> OpenAI API key
#   certs/llm_model.txt       -> LLM model name (defaults to gpt-4o-mini)
MAX_VOCABULARY_INPUT_CHARACTERS=12000
VOCABULARY_LLM_TIMEOUT_MS=30000